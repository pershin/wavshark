# WavShark

## Compile from source

Clone the source
```sh
git clone https://github.com/pershin/wavshark.git
```

Generate the configure script
```sh
cd wavshark && ./autogen.sh
```

Configure and build
```sh
./configure
make
```
